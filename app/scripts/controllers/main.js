'use strict';

/**
 * @ngdoc function
 * @name rapboardMixerApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the rapboardMixerApp
 */
angular.module('rapboardMixerApp')
  .controller('MainCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
