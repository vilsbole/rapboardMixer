'use strict';

/**
 * @ngdoc function
 * @name rapboardMixerApp.controller:AboutCtrl
 * @description
 * # AboutCtrl
 * Controller of the rapboardMixerApp
 */
angular.module('rapboardMixerApp')
  .controller('AboutCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
